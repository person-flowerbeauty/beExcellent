// function binarySearch (arr, value) {
//     let min = 0;
//     let max = arr.length - 1;
//     while (min <= max) {
//         if (value < binarySearch[min] || value > binarySearch[max]) return '未找到结果';
//         let mid = Math.ceil((min + max) / 2);
//         if (binarySearch[mid] < value) {
//             min = mid;
//         } else if (binarySearch[mid] > value) {
//             max = mid
//         } else {
//             return mid
//         }
//     };
//     return 
// };